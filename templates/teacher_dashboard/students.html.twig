{% extends 'base.html.twig' %}

{% block title %}Étudiants du cours : {{ course.title }}{% endblock %}

{% block body %}
<div class="container mt-5">
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('app_teacher_dashboard') }}">Dashboard</a></li>
        <li class="breadcrumb-item active" aria-current="page">Étudiants de {{ course.title }}</li>
      </ol>
    </nav>

    <h1 class="mb-4">Liste des Étudiants inscrits 👥</h1>
    <h3>Cours : <span class="text-primary">{{ course.title }}</span></h3>

    <div class="card shadow mt-4">
        <div class="card-body">
            {% if enrollments is empty %}
                <div class="text-center p-5">
                    <p class="display-1">🤷‍♂️</p>
                    <h4>Aucun étudiant n'est encore inscrit à ce cours.</h4>
                    <p class="text-muted">Partagez votre code de cours pour attirer des élèves !</p>
                </div>
            {% else %}
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Nom Complet</th>
                                <th>Email</th>
                                <th>Date d'inscription</th>
                                <th>Statut</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for enrollment in enrollments %}
                                <tr>
                                    <td>{{ enrollment.user.prenom }} {{ enrollment.user.nom }}</td>
                                    <td>{{ enrollment.user.email }}</td>
                                    <td>{{ enrollment.enrolledAt|date('d/m/Y H:i') }}</td>
                                    <td><span class="badge bg-success">Actif</span></td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% endif %}
        </div>
    </div>
    
    <div class="mt-4">
        <a href="{{ path('app_teacher_dashboard') }}" class="btn btn-secondary">Retour au Tableau de bord</a>
    </div>
</div>
{% endblock %}
